> **Zero-dependency requirement:** This replacement must use only the Rust standard library. No new crates may be added to `Cargo.toml`.

# Technical Directive: `tempfile`

## Project Context

- **Project:** `amber`
- **Manifest:** `./Cargo.toml`
- **Dependency kind:** Normal

## Safety Assessment

- **Classification:** Low Risk
- **Safety score:** 56/100
- **Confidence:** 65/100

## Scope Summary

Replace the normal dependency `tempfile` v3.27.0 (brings in 12 transitive dependencies). The assessment found 1 unique APIs across 33 call sites in 5 file(s). It does not appear in the public API. Classification: Low Risk (score: 56/100, confidence: 65/100).

### Affected Files
- `src/main.rs`
- `src/metadata/rustsec.rs`
- `src/analysis/usage.rs`
- `src/replacement/generator.rs`
- `src/replacement/validator.rs`

## Rationale

- Usage simplicity: 70% (1 unique APIs from 33 call sites)
- Transitive value: 45% (removes 12 transitive deps)
- Security safety: 50% (category: general)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 55% (5 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: No special category rule
- Confidence: 65% (based on usage visibility)
- Usage is non-trivial (33 call sites, 1 unique APIs); replacement requires careful validation.
- Recommended action: Generate proposal for human review

## Implementation Steps

1. Audit all usage of `tempfile` across affected files.
2. Review the generated replacement module in `amber_out/` or `amber_proposals/`.
3. Replace `tempfile::TempDir`/`NamedTempFile` with `std::env::temp_dir()` and `std::fs` helpers.
4. Ensure cleanup on drop/panic using a local RAII guard.
5. Draft a replacement PR for `tempfile` and request human review before merging.
6. Update `Cargo.toml` to remove or replace the dependency entry.
7. Run `cargo check`, `cargo clippy`, and the full test suite.

## Acceptance Criteria

- [ ] Project compiles cleanly with `cargo check --all-targets`.
- [ ] All existing tests pass with `cargo test --all-targets`.
- [ ] Every `tempfile` call site behaves identically to the original implementation.
- [ ] No new `cargo clippy` warnings are introduced.
- [ ] Memory-safety and error-handling paths are preserved.

## Testing Plan

- Run the existing test suite before any `tempfile` changes.
- Test every 33 call site(s) of `tempfile` after replacement.
- Add property-based or differential tests for edge cases.
- Benchmark compile time and binary size before/after replacement.

## Rollback Plan

- Ensure `tempfile` v3.27.0 is pinned or available in `Cargo.lock` before changes.
- Create a feature branch or draft PR for the replacement work.
- Commit after each replacement milestone to enable `git revert`.
- Keep the original dependency declaration commented during development, removing it only after validation.
- If regressions are found, restore the original `Cargo.toml` entry and revert source changes.

## Risk Notes

- Usage simplicity: 70% (1 unique APIs from 33 call sites)
- Transitive value: 45% (removes 12 transitive deps)
- Security safety: 50% (category: general)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 55% (5 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: No special category rule
- Confidence: 65% (based on usage visibility)

## Estimated Impact

- **Compile-time reduction:** -2-10% (estimated)
- **Binary-size reduction:** -50KB-500KB (estimated)

## Execution Notes

- Replaced with `src/temp.rs`.
- `TempDir` creates directories under `std::env::temp_dir()` using a process-id
  and atomic-counter suffix, and recursively deletes them on drop.
- All call sites in `src/main.rs`, `src/analysis/usage.rs`,
  `src/metadata/rustsec.rs`, `src/replacement/generator.rs`, and
  `src/replacement/validator.rs` were migrated.
- Acceptance criteria verified with `cargo check --all-targets`,
  `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets`.

---
*Generated by Amber — review before execution.*
