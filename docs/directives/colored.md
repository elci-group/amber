> **Zero-dependency requirement:** This replacement must use only the Rust standard library. No new crates may be added to `Cargo.toml`.

# Technical Directive: `colored`

## Project Context

- **Project:** `amber`
- **Manifest:** `./Cargo.toml`
- **Dependency kind:** Normal

## Safety Assessment

- **Classification:** Safe to Replace
- **Safety score:** 76/100
- **Confidence:** 80/100

## Scope Summary

Replace the normal dependency `colored` v2.2.0 (brings in 11 transitive dependencies). The assessment found 1 unique APIs across 1 call sites in 3 file(s). It appears in the public API. Classification: Safe to Replace (score: 76/100, confidence: 80/100).

### Affected Files
- `src/main.rs`
- `src/replacement/template_data/colored.rs`
- `src/reporting/formatters.rs`

⚠️ This dependency is used in the project's public API. Replacement may be a breaking change.

## Rationale

- Usage simplicity: 90% (1 unique APIs from 1 call sites)
- Transitive value: 45% (removes 11 transitive deps)
- Security safety: 65% (category: terminal)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 70% (3 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: FREQUENTLY_REPLACEABLE list match
- Confidence: 80% (based on usage visibility)
- Usage is trivial (1 call sites, 1 unique APIs), making a focused replacement feasible.
- Recommended action: Low risk - automated replacement recommended

## Implementation Steps

1. Audit all usage of `colored` across affected files.
2. Review the generated replacement module in `amber_out/` or `amber_proposals/`.
3. Replace color helpers with ANSI escape sequences or a minimal internal wrapper.
4. Respect `NO_COLOR` and `TERM` environment variables in the wrapper.
5. Apply the replacement module and replace `colored` call sites incrementally.
6. Update `Cargo.toml` to remove or replace the dependency entry.
7. Run `cargo check`, `cargo clippy`, and the full test suite.

## Acceptance Criteria

- [ ] Project compiles cleanly with `cargo check --all-targets`.
- [ ] All existing tests pass with `cargo test --all-targets`.
- [ ] Downstream consumers of `colored` in the public API continue to compile.
- [ ] Every `colored` call site behaves identically to the original implementation.
- [ ] No new `cargo clippy` warnings are introduced.
- [ ] Memory-safety and error-handling paths are preserved.

## Testing Plan

- Run the existing test suite before any `colored` changes.
- Test every 1 call site(s) of `colored` after replacement.
- Verify downstream API compatibility for public-API usage.
- Benchmark compile time and binary size before/after replacement.

## Rollback Plan

- Ensure `colored` v2.2.0 is pinned or available in `Cargo.lock` before changes.
- Create a feature branch or draft PR for the replacement work.
- Replace one module/file at a time so each step is independently revertible.
- Commit after each replacement milestone to enable `git revert`.
- Keep the original dependency declaration commented during development, removing it only after validation.
- If regressions are found, restore the original `Cargo.toml` entry and revert source changes.

## Risk Notes

- Usage simplicity: 90% (1 unique APIs from 1 call sites)
- Transitive value: 45% (removes 11 transitive deps)
- Security safety: 65% (category: terminal)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 70% (3 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: FREQUENTLY_REPLACEABLE list match
- Confidence: 80% (based on usage visibility)

## Estimated Impact

- **Compile-time reduction:** -1-2%
- **Binary-size reduction:** -10KB-30KB

## Execution Notes

- Replaced with `src/reporting/style.rs`.
- The internal `Colorize` trait returns `String` directly, so redundant
  `.to_string()` calls at colorized call sites were removed.
- `NO_COLOR` is read on every styling call (no `OnceLock` caching) so tests
  behave correctly regardless of the ambient environment.
- Acceptance criteria verified with `cargo check --all-targets`,
  `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets`.

---
*Generated by Amber — review before execution.*
