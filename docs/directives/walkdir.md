> **Zero-dependency requirement:** This replacement must use only the Rust standard library. No new crates may be added to `Cargo.toml`.

# Technical Directive: `walkdir`

## Project Context

- **Project:** `amber`
- **Manifest:** `./Cargo.toml`
- **Dependency kind:** Normal

## Safety Assessment

- **Classification:** Low Risk
- **Safety score:** 65/100
- **Confidence:** 85/100

## Scope Summary

Replace the normal dependency `walkdir` v2.5.0 (brings in 4 transitive dependencies). The assessment found 1 unique APIs across 0 call sites in 1 file(s). It does not appear in the public API. Classification: Low Risk (score: 65/100, confidence: 85/100).

### Affected Files
- `src/analysis/usage.rs`

## Rationale

- Usage simplicity: 90% (1 unique APIs from 0 call sites)
- Transitive value: 50% (removes 4 transitive deps)
- Security safety: 50% (category: general)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 80% (1 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: No special category rule
- Confidence: 85% (based on usage visibility)
- Usage is trivial (0 call sites, 1 unique APIs), making a focused replacement feasible.
- Recommended action: Generate proposal for human review

## Implementation Steps

1. Audit all usage of `walkdir` across affected files.
2. Review the generated replacement module in `amber_out/` or `amber_proposals/`.
3. Audit public API usage of `walkdir` and provide equivalent local implementations for 1 used item(s).
4. Write behavioral tests that lock the observed contract before replacing.
5. Draft a replacement PR for `walkdir` and request human review before merging.
6. Update `Cargo.toml` to remove or replace the dependency entry.
7. Run `cargo check`, `cargo clippy`, and the full test suite.

## Acceptance Criteria

- [ ] Project compiles cleanly with `cargo check --all-targets`.
- [ ] All existing tests pass with `cargo test --all-targets`.
- [ ] Every `walkdir` call site behaves identically to the original implementation.
- [ ] No new `cargo clippy` warnings are introduced.
- [ ] Memory-safety and error-handling paths are preserved.

## Testing Plan

- Run the existing test suite before any `walkdir` changes.
- Test every 0 call site(s) of `walkdir` after replacement.
- Benchmark compile time and binary size before/after replacement.

## Rollback Plan

- Ensure `walkdir` v2.5.0 is pinned or available in `Cargo.lock` before changes.
- Create a feature branch or draft PR for the replacement work.
- Commit after each replacement milestone to enable `git revert`.
- Keep the original dependency declaration commented during development, removing it only after validation.
- If regressions are found, restore the original `Cargo.toml` entry and revert source changes.

## Risk Notes

- Usage simplicity: 90% (1 unique APIs from 0 call sites)
- Transitive value: 50% (removes 4 transitive deps)
- Security safety: 50% (category: general)
- Maintenance burden: 45% (0 CVEs, score: 50)
- Testability: 80% (1 affected files)
- API surface: 70% (using ~20% of public APIs)
- Category rule applied: No special category rule
- Confidence: 85% (based on usage visibility)

## Estimated Impact

- **Compile-time reduction:** -2-10% (estimated)
- **Binary-size reduction:** -50KB-500KB (estimated)

## Execution Notes

- Replaced with `src/analysis/walker.rs`.
- The walker yields only files (directories are expanded and skipped), matching
  the previous `.extension().is_some_and(|e| e == "rs")` filter behavior.
- Symlinks are not followed, matching `walkdir` defaults.
- Acceptance criteria verified with `cargo check --all-targets`,
  `cargo clippy --all-targets -- -D warnings`, and `cargo test --all-targets`.

---
*Generated by Amber — review before execution.*
